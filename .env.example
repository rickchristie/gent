# AI Provider API Keys for Testing
# Copy this file to .env and fill in your keys:
#   cp .env.example .env

# OpenAI
GENT_TEST_OPENAI_KEY=

# Anthropic
GENT_TEST_ANTHROPIC_KEY=

# Google (Gemini)
GENT_TEST_GOOGLE_KEY=

# xAI (Grok)
GENT_TEST_XAI_KEY=

# AWS Bedrock
GENT_TEST_AWS_ACCESS_KEY_ID=
GENT_TEST_AWS_SECRET_ACCESS_KEY=
GENT_TEST_AWS_REGION=

# GitHub Copilot Pro+ / GitHub Models
# Uses the GitHub Models API (models.github.ai) which is OpenAI-compatible.
# Requires a fine-grained Personal Access Token with models:read permission.
#
# How to create the token:
#   1. Go to https://github.com/settings/personal-access-tokens/new
#      (or: Settings > Developer settings > Personal access tokens > Fine-grained tokens)
#   2. Set "Token name" to something descriptive (e.g. "gent-github-models")
#   3. Set "Expiration" to your preference (shorter is more secure)
#   4. Under "Resource owner", select your personal GitHub account
#   5. Under "Repository access", select "Public Repositories (read-only)" or none
#   6. Expand the "Account permissions" section (NOT Repository or Organization)
#   7. Find "Models" and set it to "Read-only"
#   8. Click "Generate token" and copy it immediately (it's shown only once)
#
# The token will look like: github_pat_...
# All GitHub accounts get free rate-limited access. Copilot Pro+ gets higher limits.
GENT_TEST_GITHUB_TOKEN=

# Ollama (local, usually no key needed)
GENT_TEST_OLLAMA_HOST=http://localhost:11434
